# Cookbook

The copy-paste templates live in [docs/guide/cookbook.md](../../guide/cookbook.md).

It collects the frequent snippets:

- command handler with in-process events
- transactional save + enqueue
- idempotent consumer
- tenant propagation
- HTTP route over a handler (Axum)
