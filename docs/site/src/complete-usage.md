# Complete Usage Guide

For an end-to-end walkthrough (from domain model to production operations), see:

- `docs/guide/complete-usage.md`

This includes:

- dependency selection by feature set;
- aggregate modeling with optimistic concurrency;
- command/query wiring patterns;
- transactional outbox usage;
- idempotent consumer flow;
- multi-tenant repository usage;
- production validation gate.
